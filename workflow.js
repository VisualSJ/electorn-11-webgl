const { fstat } = require("fs");
new Promise((resolve) => {
    console.log('mmmmmmmmmmmmmm')
    // require('fs').writeFileSync('./tt.log', '123');
    // process.exit(-1);
    setTimeout(resolve, 10000);
});